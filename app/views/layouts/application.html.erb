<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "креативы" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Креативы" %>">
    <%= stylesheet_link_tag "application" %>
    <%# Foundation 5 won't work with Rails Turbolinks %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <div class="row">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div>
    <%# Foundation 5 won't work when application.js is included in HEAD %>
    <script type="text/javascript" src="markitup/jquery.markitup.js"></script>
    <script type="text/javascript" src="markitup/sets/markdown/set.js"></script>
    <%= javascript_include_tag "application" %>
  </body>
</html>
