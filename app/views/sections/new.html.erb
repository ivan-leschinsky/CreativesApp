<div class="row">
  <h1>Новая глава</h1>
  <fieldset>
    <%= form_for @section, url: creative_sections_path do |f| %>
      <% if @section.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>

          <ul>
          <% @section.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.text_field :name, :placeholder => "Имя главы" %>
      </div>
      
      <div class="row">
        <div class="medium-6 columns">
          <div class='wmd-panel'>
            <div id='wmd-button-bar'></div>
            <%= text_area_tag :body, "", :name => "section[body]", :class => "wmd-input", :id => "wmd-input" %>
          </div>
        </div>
        <div class="medium-6 columns">
          <p>Предпросмотр:</p>
          <div id="wmd-preview" class="wmd-panel wmd-preview"></div>      
        </div>
      </div>
      
      <div class="field">
        <%= f.hidden_field :creative_id %>
        <%= f.hidden_field :number %>
      </div>
      <div class="actions">
        <button type="submit" class="tiny button success radius">Сохранить </button>
      </div>
    <% end %>
  </fieldset>
  <%= link_to t('back'), creative_path(@section.creative), :class => "tiny button secondary" %>
</div>
