<div class="row">
<div class="large-12 columns">
<%= link_to 'Новый креатив', new_creative_path, class: "button radius right" %>
<h1 align="center">Все креативы</h1>

</div>
    <% @creatives.each do |creative| %>
    <div class="medium-6 columns">
      <div class="panel">      
        <h3>
        <h3>
          "<%= link_to creative.name, creative %>"
        </h3>
        <%= creative.description %><br/>
        <p><%= creative.sections.size %> глав</p>
        <ul class="button-group round">
          <li><%= link_to t('show'), creative, class: "small button success"%></li>
          <li><%= link_to t('edit'), edit_creative_path(creative), class: "small button" %></li>
        <li><%= link_to t('destroy'), creative, method: :delete, data: { confirm: t('are_you_sure')}, class: "small button alert" %></li>
        </ul>
      </div>        
      </div>
    <% end %>

<hr /><hr />
 <% @creatives.each do |creative| %>
 <div class="medium-6 columns">
      <ul class="pricing-table">
  <li class="title"><%= link_to creative.name, creative %></li>
  <li class="price"><%= creative.description %></li>
  <li class="description">
  <span class="label radius">Программирование</span>
  <span class="secondary label radius">Спорт</span>
  <span class="success label radius">Еще что-то</span>
  <span class="alert label round">И еще...</span>
</li>
  <li class="bullet-item"><%= creative.sections.size %> глав</li>
  <li class="bullet-item">комментариев</li>
  <li class="cta-button">
  <%= link_to t('show'), creative, class: "tiny button success radius"%>
          <%= link_to t('edit'), edit_creative_path(creative), class: "tiny button radius" %>
        <%= link_to t('destroy'), creative, method: :delete, data: { confirm: t('are_you_sure')}, class: "tiny button alert radius" %>
  
  </li>
</ul>
</div>
    <% end %>
